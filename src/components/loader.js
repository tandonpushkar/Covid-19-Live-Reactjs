import React from "react";

const Loader = () => {
  return (
    <img
      alt="text"
      src={require("./images/loader.gif")}
      width="50"
      height="50"
    />
  );
};

export default Loader;
